Notification Service (placeholder)

Handles sending emails (OTP, welcome, reminders). Should be async and consume events (Kafka/RabbitMQ). Also expose internal endpoint for other services to request sends.

Port: 4004

Placeholder for now; can implement SMTP integration and event consumer on request.
